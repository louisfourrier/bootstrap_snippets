<% provide(:title, "TITLE") %>
<% provide(:description, "Description" ) %>
<% provide(:keywords, "keyword1, keyword2")%>

<h1 class="page-header text-center">Editing Tag</h1>

<%= render 'form', tag: @tag %>

<%= link_to 'Show', @tag, :class => "btn btn-warning" %> |
<%= link_to 'Back', tags_path, :class => "btn btn-info" %>