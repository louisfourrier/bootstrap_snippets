<div class="navbar navbar-default navbar-fixed-top navbar-nadvence" role="navigation">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="<%= root_path %>">Bootstrap snippets</a>
		</div>
		<div class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
				<li class="nadvence-family">
					<%= link_to "Snippets", snippets_path%>
				</li>
				<li class="nadvence-family">
					<%= link_to "Tags", tags_path%>
				</li>

				<% if current_user %>
				<li class="nadvence-family">
					<%= link_to "Your profile", profile_user_path(current_user) %></a>
				</li>
				<% end %>
				
				<% if is_administrator %>
				<li class="nadvence-family">
					<%= link_to "Administration", administration_home_path %></a>
				</li>
				<% end %>

			</ul>

			<% if current_user %>
			<ul class="nav navbar-nav navbar-right">
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown"> <span class="glyphicon glyphicon-user"></span>  <strong><%= current_user.pseudo %></strong> <span class="glyphicon glyphicon-chevron-down"></span> </a>
					<ul class="dropdown-menu">
						<li>
							<%= link_to "Settings", edit_registration_path(current_user) %>
						</li>
						<li>
							<%= link_to "New Snippet", new_snippet_path%>
						</li>
						<li>
							<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
						</li>
					</ul>
				</li>
			</ul>

			<% else %>

			<ul class="nav navbar-nav navbar-right">
				<li>
					<%= link_to "Register", new_user_registration_path %>
				</li>
				<li>
					<%= link_to "Login", new_user_session_path %>
				</li>

			</ul>

			<% end %>

		</div><!--/.nav-collapse -->
	</div>
</div>