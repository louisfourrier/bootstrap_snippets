<% provide(:title, "TITLE") %>
<% provide(:description, "Description" ) %>
<% provide(:keywords, "keyword1, keyword2")%>

<h1 class="page-header text-center">Users</h1>
<ul class="nav nav-pills nav-stacked tag-list">
	<% @users.each do |user| %>
		<li class="col-md-4 col-sm-6 col-xs-12 col-lg-3 ">
			<%= link_to user do %>
			<span class="badge pull-right"><%= user.snippets.count %></span>
			<%= user.pseudo %>
			<%end %>
		</li>
	<% end %>
</ul>


<br>

<%= link_to 'New User', new_user_path, :class => "btn btn-success" %>